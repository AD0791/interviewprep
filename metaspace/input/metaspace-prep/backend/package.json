{
  "name": "metaspace-prep-backend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Interview prep: the on-chain/off-chain integration layer of a Web3 game backend.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "indexer": "node --experimental-strip-types src/indexer.ts",
    "server": "node --experimental-strip-types src/server.ts"
  },
  "dependencies": {
    "ethers": "^6.13.4",
    "fastify": "^5.1.0",
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "@types/pg": "^8.11.10",
    "typescript": "^5.7.2"
  }
}
